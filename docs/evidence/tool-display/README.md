# 工具调用的口语显示

2026-09-12，主 Agent inline 实现与审查，接续 `1a93f0d`。

- 通过原生 `conversation.chat.node` 的 `tool-call`、`turn-process` 注册显示，保留原生记录、事件顺序、折叠控制和子调用树。卸载时原生渲染恢复。
- 当前50个工具都有动作说明；未知工具也只显示通用动作，不把工具名/参数搬到收起的行。
- 正在执行、完成、失败、中断分别表述；成功提案仅表示准备好，后台委托仅表示已派出。
- 展开才装载工具名、原始参数和完整结果；保留原生轨迹/定义查看入口。图片仍使用原生附件预览，不把图片结果替换成纯JSON。
- 技术结果没有改写或过滤。只是它们的显示位置从默认摘要移入展开详情。

验证：build/tests类型 PASS；4单元 PASS；3个不同浏览器场景 PASS（工具展开与轨迹回跳、原生图片预览、提案仍按回复内联）。截图初次落在原生移动右栏过渡状态，已改成从手机尺寸启动再检查真实详情可见；`mobile.log`和`tool-details-mobile.png`为最终证据。64004实际模块更新后，真实课堂13条历史调用均显示口语摘要、初始详情DOM为0；展开真实目录调用显示propose_skeleton/参数/结果/原生查看入口。未向实际课堂发送消息或确认提案。

对应代码：`packages/client/src/classroom/{ToolActivity.tsx,tool-copy.ts,tool-activity.css}`。
