# 0.8.1 浏览器定向验证

2026-09-22，Codex 内置浏览器，独立 Host `http://127.0.0.1:50004/`。LLM 使用脚本化 Vault 测试适配器；会话、Bash 执行与批准是实际 DSH 链路。没有以合成回复作为真实教学能力证据。

| 操作 | 实际观察 | 结果 |
| --- | --- | --- |
| 打开“学习步骤展示 · 合成演示”，展开“1 次工具调用” | `heading "记录这次作答并安排复习" [level=3]`；`status: 已返回`；执行详情未展开；标题无 `[notara:review-record]` / `###` | PASS |
| 展开/收起执行详情 | 命令与合成输出显示，再折叠时隐藏；保留“查看原始记录”入口 | PASS |
| 发送“演示批准流程” | 标题“查看这道题的相关资料”、状态“处理中”；原生批准区显示完整命令、“拒绝”和“允许一次” | PASS |
| 点击“允许一次” | 命令返回、完成该轮，原生工具过程自动折叠 | PASS |
| 发送“演示执行失败”并允许一次 | 实际执行 `exit 7`；展开过程后标题“保存这次课堂笔记”旁显示“执行失败”，详情有 `(no output)` 和 `[exit code: 7]` | PASS |
| 发送“演示原生回退”并允许一次 | 未标记 description 保留原生“Bash 普通命令演示”，可展开原生终端卡片、复制和查看入口 | PASS |
| 读取浏览器 error/warn | 返回空列表，无 slot 组件初始化异常 | PASS |
| 打开单独的“对话美化 · 示例” | 标题、状态、默认折叠详情与原生正文同屏，侧栏保留此页供查看 | PASS |

两条成功示例仅执行 `printf` 输出“这是合成演示输出，未写入真实学习记录。”，未模拟真实复习写回成功。
