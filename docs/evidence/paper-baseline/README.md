# 横线与文字基线

2026-09-12，接续 `1a93f0d`，与工具口语显示一起收口。

纸纹以原生正文 flow 的实际坐标为起点，随原生滚动移动。每段依据实际字体基线和实际位置补齐偏移，段距使用整行；表格行高与留白按纸行计，整表基线校准而不移动各个单元格；输入框独立校准。便签和技术详情保留自己的底色与排版。滚动仅更新纸纹位置，字号/字体/展开或尺寸变化才重算文字。

| 验证 | 结果 |
|---|---|
| 最终build/tests类型 | PASS，`build.log` / `types.log` |
| 正文实际基线、滚动113px、大字号/另一字迹、展开收起便签 | PASS，`final-browser.log` |
| 每个表格单元格首行实际基线、表格居中/局部滚动、纸底/白色主题/字体切换 | PASS，`final-browser.log` |
| 工具详情/原生轨迹回跳、原生图片预览 | PASS，`browser.log` |
| 误差 | 用浏览器内零高度inline标记读取真实基线，正文和表格误差均小于0.8px |
| 真实64004页面 | PASS；文楷/横线纸；滚动后flowTop=-4535、paperOffset=-4539px（保留4px基线位置），两表中心偏差均0；工具详情均收起，右栏恢复收起；正文落线目检 |

截图：`aligned-page.png`、`aligned-table.png`。输入框用隔离测试输入验证，未改真实课堂输入。真实模型新调用未运行。
