# Vault 教学总合同补全

## 目标

按用户提供的压缩前清单补齐基础教学范围，与后续记忆、路线/日志和图谱设计统一，并列出剩余产品决策。

## 已完成的文档工作

- 新增 `docs/migration/2026-09-21-vault-teaching-migration.md`，覆盖原生预设、三默认教法、本课设置、资料/PDF/图片读取、同课讲练、三任务 Skill、可靠写回、简约 UI、A/B/C 全链验收。
- 更新 `docs/migration/README.md`，将总合同和专题列为当前入口，历史基线单列。
- 更新记忆与路线专题的总合同引用、三默认教法、设置范围与本轮完整迁移约定。
- 重写旧视图设计中的过期布局要求：资产页统一阅读编辑、原生对话单 composer、workspace 分屏、节点带入对话、真实子卡详情；保留标签聚合/邻接与独立路线要求。
- 两个只读子 Agent 核对设置/资源和资料/媒体接缝，主 Agent 复核关键源码；独立审查覆盖当前文档间的一致性。

## 用户本轮已拍板

1. 本轮迁移全部完成 A+B+C，按基础教学 → 记忆 → 路线与日志分阶段验收。
2. 只迁功能与教学资源，使用现有 Vault，不导入旧运行环境的学习数据。

默认教学、原生写批准、无互动 HTML 等此前决定未重新询问。当前没有需要再次拍板的产品缺口。

## 关键核对

- `CourseMetadataSchema` / `CoursePatchSchema` 已有三个设置字段，但学习目标清除语义须补齐；旧 domain 持久化不等于新组合已经接入。
- 原生引用 `registerVaultReference` 能把显式 PDF 文本带入对话；缺的是教师自主按页/选区读取与真实图像能力。不能把 UI 能显示误当模型读取已完成，也不能反过来说文本完全不可达。
- 原 `fullDocument` 拼接整份 PDF 的路径需要有界化。
- Skill 正文仍是迁移来源，本轮未改提示词或运行代码；新合同要求清除旧提案/独立回合/复习等尚未迁移的可调用说明。
- 独立审查后明确资料模型 IO 的单点注册归属、结构分析正文待新增、首条模型请求的设置验收，以及 C03 的固定小结目标/单次操作去重。审查中引用到的旧小结键表述已过期，主 Agent 以当前路线合同核对，不把 session 目标身份误当成每次保存操作的同一幂等键。

## 验证边界

PASS（文档层）：源码核对、独立审查与主 Agent 修订、Python 内联格式/本地链接/范围检查、`git diff --check`。未运行构建、产品测试、浏览器或真实模型。未启动实例、未读取真实学习数据、未提交；保留 Vault 工作树原有改动。

下一入口：总合同第 7 节实施顺序与第 8 节 A/B/C 验收；阶段通过不代替完整范围完成。
