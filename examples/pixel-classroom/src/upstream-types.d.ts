// Include upstream ambient declarations for its optional diagnostics without
// installing the test harness or changing the vendored renderer at runtime.
import type {} from '../upstream/webview-ui/src/testHooks';
