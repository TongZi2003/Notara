# 论证工作台

插件页安装本目录或 `npm pack` 生成的 tgz，课堂顶部打开「论证工作台」。

按主题填写观点、证据与理由、反例与疑问、修订结论。可只完成当前阶段。点击「保存草稿」后刷新可恢复；「整理为笔记」进入宿主完整确认，保存后归原有笔记本所有，卸载仍保留。

在＋或斜杠选择「检验我的论证」，把要讨论的部分发进对话，或保存笔记后让老师读取。工作台不自动把未保存内容发给模型，也不模拟独立多智能体结果。

主题、控件字号与字体跟随宿主。无外部网络、依赖、原生代码；只申请 `draft` 与 `save-note` 权限。
