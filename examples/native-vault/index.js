/**
 * The clean native DSH prototype intentionally has no StudyForge dependency.
 * Host-side filesystem and agent bridges will be added only after the surface
 * is judged in the bare Harness.
 */
export function apply() {}
